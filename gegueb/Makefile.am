pkg = gegueb
pkgconfigdir=$(libdir)/pkgconfig
pkgconfig_DATA=gegueb-sharp.pc

SYMBOLS = gegueb-symbols.xml
INCLUDE_API =
METADATA = Gegueb.metadata
references =
glue_includes =

sources = Application.cs

add_dist = gegueb-sharp.pc.in

include ../Makefile.include
